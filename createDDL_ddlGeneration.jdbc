CREATE TABLE QUESTAO (ID  SERIAL NOT NULL, PERGUNTA VARCHAR(100), PRIMARY KEY (ID))
CREATE TABLE ALTERNATIVA (ID  SERIAL NOT NULL, ALTERNATIVA VARCHAR(255), QUESTAO_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE ALTERNATIVA ADD CONSTRAINT FK_ALTERNATIVA_QUESTAO_ID FOREIGN KEY (QUESTAO_ID) REFERENCES QUESTAO (ID)
